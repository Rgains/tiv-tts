# Streamlit Community Cloud checks the app directory before the repository
# root. Keep this file beside demo_app.py so it takes precedence over the root
# uv.lock, whose default environment resolves only the lightweight tiv-tts
# package and would leave every third-party import missing.
#
# Keep these runtime dependencies in sync with the demo and research groups in
# ../pyproject.toml.
#
# CPU-only wheels: the default PyPI torch build pulls bundled CUDA libraries
# that a CPU-only host cannot use and does not have room for.
--extra-index-url https://download.pytorch.org/whl/cpu

coqui-tts==0.27.5
huggingface-hub>=0.36
numpy>=1.26,<2
soundfile>=0.13
torch>=2.7,<2.9
torchaudio>=2.7,<2.9
torchvision>=0.22,<0.24
transformers>=4.57,<5
streamlit>=1.56.0
