seed: 202604
dataset_root: Tiv-TTS-Dataset
audit_csv: outputs/dataset_audit/samples.csv
work_dir: data/interim/tiv_smoke
output_dir: outputs/smoke_test

data:
  sample_count: 10
  source_group: null
  minimum_duration_seconds: 2.0
  maximum_duration_seconds: 6.0

audio:
  sample_rate: 22050
  n_fft: 1024
  hop_length: 256
  n_mels: 80
  f_min: 0.0
  f_max: 8000.0
  griffin_lim_iterations: 16

model:
  embedding_dim: 48
  hidden_dim: 64

training:
  batch_size: 2
  learning_rate: 0.002
  initial_steps: 30
  resume_steps: 5
  log_every: 5

